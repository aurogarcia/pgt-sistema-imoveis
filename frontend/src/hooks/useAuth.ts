import { useContext } from 'react';
import { AuthContext } from '../contexts/AuthContext';

// Este hook é apenas um re-export do contexto para manter consistência
// A implementação principal está no AuthContext
export { useAuth } from '../contexts/AuthContext';